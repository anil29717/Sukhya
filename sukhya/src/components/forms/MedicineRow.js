import { View, Text, StyleSheet } from 'react-native';

export default function MedicineRow() {
  return (
    <View style={styles.container}>
      <Text>MedicineRow</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1 },
});
