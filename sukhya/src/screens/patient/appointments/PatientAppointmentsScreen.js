import { View, Text } from 'react-native';

export default function PatientAppointmentsScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>PatientAppointmentsScreen</Text>
    </View>
  );
}
